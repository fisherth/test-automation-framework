package com.notsruht.taf.config;

import org.springframework.test.context.ContextConfiguration;

/**
 * Application context for the Test Automation Framework
 *
 */
@ContextConfiguration(classes={TAFSpringConfiguration.class})
public class TAFContextConfiguration {
	
}